Чтобы подключить application.properties, нужно внести изменения в конфигурацию:

🔧 1. Создай файл src/main/resources/application.properties
Создай папку src/main/resources (если нет) и файл application.properties:

text
src/
└── main/
    └── resources/
        ├── application.properties  ← СОЗДАТЬ
        └── hibernate.properties    ← УЖЕ ЕСТЬ
Содержимое application.properties:

properties
# ========== SPRING LOGGING ==========
logging.level.org.springframework=DEBUG
logging.level.org.springframework.beans.factory=TRACE
logging.level.org.springframework.context.annotation=DEBUG
logging.level.org.springframework.transaction=DEBUG
logging.level.org.springframework.orm=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.jdbc=DEBUG

# ========== FLYWAY LOGGING ==========
logging.level.org.flywaydb=DEBUG
logging.level.org.flywaydb.core=TRACE
logging.level.org.flywaydb.core.internal.database=TRACE

# ========== HIBERNATE LOGGING ==========
logging.level.org.hibernate=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ========== YOUR APP LOGGING ==========
logging.level.org.weather=TRACE

# ========== DATASOURCE DEBUG ==========
spring.datasource.debcp.test-on-borrow=true
spring.datasource.debcp.validation-query=SELECT 1
🔧 2. Обнови SpringConfig.java
Добавь второй @PropertySource:

java
@Configuration
@ComponentScan("org.weather")
@PropertySource({"classpath:hibernate.properties", "classpath:application.properties"}) // ← ДВА ФАЙЛА
@EnableTransactionManagement
@EnableJpaRepositories("org.weather.repository")
@EnableWebMvc
public class SpringConfig implements WebMvcConfigurer {
    // ... остальной код без изменений ...
🔧 3. Альтернативный способ - через Environment
Если не работает @PropertySource, добавь явное чтение свойств:

java
@Bean
public static PropertySourcesPlaceholderConfigurer propertyConfig() {
    PropertySourcesPlaceholderConfigurer configurer = new PropertySourcesPlaceholderConfigurer();
    configurer.setLocations(
        new ClassPathResource("hibernate.properties"),
        new ClassPathResource("application.properties")
    );
    return configurer;
}
🔧 4. Добавь логирование в Flyway бин
java
@Bean(initMethod = "migrate")
public Flyway flyway(DataSource dataSource) {
    System.out.println("=== FLYWAY DEBUG ===");
    System.out.println("Configuring Flyway with datasource: " + dataSource);

    try {
        Flyway flyway = Flyway.configure()
                .dataSource(dataSource)
                .locations("classpath:db/migration")
                .baselineOnMigrate(true)
                .group(true)
                .outOfOrder(true)
                .ignoreMigrationPatterns("*:pending")
                .load();

        System.out.println("=== FLYWAY MIGRATION STARTED ===");
        return flyway;
    } catch (Exception e) {
        System.err.println("=== FLYWAY CONFIGURATION ERROR ===");
        e.printStackTrace();
        throw e;
    }
}
📁 Структура проекта после изменений:
text
src/
└── main/
    ├── java/
    │   └── org/weather/
    │       ├── config/
    │       │   └── SpringConfig.java
    │       └── controller/
    │           └── AuthController.java
    └── resources/
        ├── application.properties  ← НОВЫЙ ФАЙЛ
        ├── hibernate.properties    ← СУЩЕСТВУЮЩИЙ
        └── db/
            └── migration/
                └── V1__Create_users_table.sql
🎯 Проверка что файлы подключились:
Добавь в любой бин для проверки:

java
@Bean
public String checkProperties(Environment env) {
    System.out.println("=== CHECKING PROPERTIES ===");
    System.out.println("Hibernate URL: " + env.getProperty("hibernate.connection.url"));
    System.out.println("Logging Level: " + env.getProperty("logging.level.org.springframework"));
    return "properties-check";
}
⚠️ Если Maven не видит resources:
Добавь в pom.xml:

xml
<build>
    <resources>
        <resource>
            <directory>src/main/resources</directory>
        </resource>
    </resources>
</build>
После этих изменений логирование заработает и ты увидишь детальную информацию об ошибке Flyway! 🚀

